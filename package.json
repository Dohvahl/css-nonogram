{
  "name": "css-nonogram",
  "version": "1.0.0",
  "description": "A playable nonogram in pure HTML and CSS, with a build-time board generator.",
  "type": "module",
  "scripts": {
    "gen": "node puzzle-gen/generate.ts",
    "test": "node --test puzzle-gen/clues.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/node": "^24.0.0"
  }
}
